# CardKeep Dashboard

Static dashboard for CardKeep capture + scan-funnel analytics.

Reads from a Google Apps Script endpoint bound to a private Google Sheet;
renders totals, card-type breakdown, recent captures, and a scan-funnel
drop-off view.

Deployed via GitHub Pages — see `index.html`.
